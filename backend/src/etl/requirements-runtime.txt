# Dependências mínimas pra rodar em produção (Dockerfile do backend) os
# extractors da whitelist de disparo via interface — ver
# backend/src/utils/extractoresElegiveis.ts. NÃO é o requirements.txt
# completo do ambiente de desenvolvimento (esse continua a fonte de verdade
# pra rodar QUALQUER extractor localmente, geopandas incluído — nenhum
# extractor da whitelist usa geopandas, então ele nunca entra aqui). Sem pin
# de versão de propósito, mesmo critério já usado no venv de diagnóstico
# desta sessão — deixa o pip resolver a versão compatível com o Python da
# imagem em vez de arrastar um pin testado só no Windows local.
#
# pandas/numpy/sqlalchemy/psycopg2-binary/requests: base, todos os scripts.
# pyarrow: só mmgd_aneel.py, lê Parquet (pd.read_parquet exige pyarrow ou
#   fastparquet, nenhum dos dois vem com o pandas sozinho).
# google-cloud-bigquery/db-dtypes: só extrair_renda_trabalho_rais.py e
#   extrair_capital_humano_mortalidade_infantil.py (db-dtypes é exigido pelo
#   próprio to_dataframe() do client BigQuery pra converter certos tipos).
#   Essas duas fontes SÓ funcionam disparadas pela interface se
#   GOOGLE_APPLICATION_CREDENTIALS_JSON estiver configurada na Railway (ver
#   docs/DEPLOY_TEMPORARIO.md, Seção 10) — sem isso, falham de forma visível
#   no log (erro de autenticação), não travam nem corrompem nada.
# pypdf: só extrair_reforma_casa_brasil_solar.py (lê o PDF enviado por upload).
# shapely/pyproj: seed_zeis_sao_paulo.py e seed_zeis_belo_horizonte.py (também
#   enviados por upload) — processam geometria (WKB/reprojeção) SEM precisar
#   de geopandas/GDAL completo, shapely e pyproj têm wheel próprio.
# openpyxl: extrair_geracao_eletrica_nacional_epe.py e
#   extrair_geracao_mmgd_epe_pdgd.py (também via upload) — leem .xlsx.
pandas
numpy
sqlalchemy
psycopg2-binary
requests
pyarrow
google-cloud-bigquery
db-dtypes
pypdf
shapely
pyproj
openpyxl
